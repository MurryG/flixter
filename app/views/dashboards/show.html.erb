<br />

<div class="booyah-box col-10 offset-1">
  <h1 class="text-center">What I'm Learning</h1>
  <br />
  <hr class="col-8 offest-2">
  <br class="clr" />
  <% if !current_user.enrolled_courses.exists? %>
    <div class="col-12 text-center">
      <p>You aren't enrolled in any courses yet.</p>
      <p>Consider taking a look at our <%= link_to 'variety of courses', new_instructor_course_path %> and enrolling in something! It's always a good day to learn something new.</p>
    </div>
  <% else %>
    <% current_user.enrolled_courses.each do |course| %>
      <h3 class="text-center">
        <%= link_to course.title, course_path(course) %>
      </h3>
      <br />
      <%= image_tag course.image.url, class: 'img-fluid' unless course.image.url.nil? %>
      <br />
      <div class="row">
        <div class="col-12 col-sm-8">
          <%= course.description %>
        </div>
        <div class="col-12 col-sm-4 text-center">
          <%= link_to "Go to Course Page", course_path(course), class: 'btn btn-large btn-primary' %>
        </div>
      </div>
      <hr class="col-6 offest-3">
      <br class="clr" />
    <% end %>
  <% end %>

  <h1 class="text-center">What I'm Teaching</h1>
  <br />
  <hr class="col-8 offest-2">
  <br class="clr" />
    <% if !current_user.courses.exists? %>
    <div class="col-12 text-center">
      <p>You haven't created any courses yet.</p>
      <p>Is there something you're good at? Consider <%= link_to 'teaching something!', new_instructor_course_path %></p>
    </div>
  <% else %>
    <% current_user.courses.each do |course| %>
      <h3 class="text-center">
        <%= link_to course.title, course_path(course) %>
      </h3>
      <br />
      <%= image_tag course.image.url, class: 'img-fluid' unless course.image.url.nil? %>
      <br />
      <div class="row">
        <div class="col-12 col-sm-8">
          <%= course.description %>
        </div>
        <div class="col-12 col-sm-4 text-center">
          <%= link_to "Go to Course Page", course_path(course), class: 'btn btn-large btn-primary' %>
        </div>
      </div>
      <hr class="col-6 offest-3">
      <br class="clr" />
    <% end %>
  <% end %>
</div>